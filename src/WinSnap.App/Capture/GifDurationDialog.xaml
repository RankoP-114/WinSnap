<Window x:Class="WinSnap.App.Capture.GifDurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="录制 GIF"
        Width="320"
        Height="210"
        MinWidth="320"
        MinHeight="210"
        MaxWidth="320"
        MaxHeight="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        SnapsToDevicePixels="True"
        FontSize="14">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="录制秒数" FontWeight="SemiBold" />
        <TextBox x:Name="DurationTextBox"
                 Grid.Row="1"
                 Height="32"
                 Margin="0,8,0,4"
                 VerticalContentAlignment="Center"
                 InputMethod.IsInputMethodEnabled="False" />
        <TextBlock Grid.Row="2"
                   Text="范围：1 到 60 秒"
                   Foreground="#FF777777" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定"
                    IsDefault="True"
                    Width="78"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="OnOkClick" />
            <Button Content="取消"
                    IsCancel="True"
                    Width="78"
                    Height="30"
                    Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</Window>
